services:
    server:
        build: .
        ports:
            - "8080:8080"
    client:
        build:
            context: .
            dockerfile: client.Dockerfile
        environment:
            - GH_SEARCH_SERVER_ADDRESS=server
        depends_on:
            - "server"